[build]
  command = "pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  # Leave unset so the gallery stays off without local preview media.
  # PUBLIC_ANIMATION_LIBRARY_ENABLED = "false"

# Optional Lighthouse reports in the Netlify UI:
# [[plugins]]
#   package = "@netlify/plugin-lighthouse"
